# vitest.workspace.ts — DEPRECATED

> **Do not create this file.** The separate `vitest.workspace.ts` was deprecated in Vitest v3.2 and is not supported in Vitest v4+.
>
> Use `test.projects` inline in `vitest.config.ts` instead. See [vitest-config.md](vitest-config.md) for the full configuration including the three test projects.
